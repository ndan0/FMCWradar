# FMCWradar

Instructions:
Open Matlab, addpath to directory containing auto_phase_array.m, cfar_detection_paper.m
run auto_phase_array (there are no input variables)

To change the target parameters, see the variables in lines 1-11 (target_dist, target_speed, target_az, target_rcs) where each item in the array is a separate target.